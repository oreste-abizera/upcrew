import React from 'react'
import { Redirect } from "react-router-dom";
export default function DevelopersPage() {
    return (
      <div>
        <Redirect to="/"></Redirect>
        hello from DevelopersPage
      </div>
    );
}
